Place index.html, script.js and assets/ under this /public directory.
Server serves this folder and exposes POST /api/contact to send mail to Outlook.
Update index.html form's data-endpoint if you deploy the API on a different origin.
